import { FunctionComponent } from 'react';

const PleaseLogin: FunctionComponent = () => {

    return (
        <div className="mt-4 flex justify-center bg-danger">
            <h3>Bitte erst einloggen</h3>
        </div>
    );
};

export default PleaseLogin;